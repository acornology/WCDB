# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for Demo
  pod 'WCDB', :path => '.'
  pod 'DoraemonKit', :git => 'git@github.com:didi/DoraemonKit.git', :branch => 'master', :subspecs => ['Hook']

end
